import { SelectTasksPipe } from './select-tasks.pipe';

describe('SelectTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
