import { ArchiveTasksPipe } from './archive-tasks.pipe';

describe('ArchiveTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new ArchiveTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
